<?php
declare(strict_types=1);
require_once __DIR__.'/../../config/app.php';require_once __DIR__.'/../../includes/security.php';require_once __DIR__.'/../../includes/auth.php';require_once __DIR__.'/../../includes/otp.php';require_post();
try{$identity=request_string('identity',254);$public='If the details are associated with an account, a verification code will be sent.';if($identity==='')json_response(false,'Enter your email address or mobile number.',[],422);$u=find_user_by_identity($identity);if(!$u||$u['ACCOUNT_STATUS']!=='ACTIVE')json_response(true,$public);$type=$u['EMAIL']?'EMAIL':'MOBILE';$sent=$type==='EMAIL'?$u['EMAIL']:$u['COUNTRY_CODE'].$u['MOBILE_NUMBER'];$otp=create_otp((int)$u['USER_ID'],$type,$sent);$data=APP_ENV!=='production'?['development_otp'=>$otp,'user_id'=>(int)$u['USER_ID'],'otp_type'=>$type]:[];json_response(true,$public,$data);}catch(Throwable $e){error_log('Forgot password: '.$e->getMessage());json_response(false,'Unable to process the recovery request right now.',[],500);}
